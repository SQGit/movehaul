<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme1" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ColorStatus</item>
        <item name="colorPrimaryDark">@color/ColorStatus</item>
        <!--<item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>-->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="tab" parent="Material.Widget.TabPageIndicator">
        <item name="tpi_tabPadding">12dp</item>
        <item name="tpi_tabRipple">@style/Material.Drawable.Ripple.Wave</item>
        <item name="tpi_indicatorHeight">2dp</item>
        <item name="tpi_indicatorAtTop">false</item>
        <item name="tpi_tabSingleLine">true</item>
        <item name="tpi_centerCurrentTab">false</item>
        <item name="android:textColor">@drawable/color_state</item>
        <item name="android:textStyle">bold</item>
        <item name="tpi_mode">scroll</item>
    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/gold</item>
    </style>

</resources>
